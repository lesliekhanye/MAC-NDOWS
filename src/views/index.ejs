<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to MAC-NDOWS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" type="image/x-icon" href="/images/macndows.png">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1a1a1a 0%, #2c2c2c 100%);
    }
    .minion-btn {
      background: linear-gradient(135deg, #f9dc5c 0%, #f7d039 100%);
      color: #000;
      transition: all 0.3s ease;
    }
    .minion-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 6px rgba(249, 220, 92, 0.3);
    }
    .logo {
      max-width: 180px;
      filter: drop-shadow(0 4px 6px rgba(255, 255, 255, 0.1));
    }
    .glass-effect {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
  </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen p-4 text-center text-gray-200">
  <div class="w-full max-w-4xl glass-effect rounded-2xl p-8 space-y-8">
    <!-- Logo -->
    <img src="./images/macndows.png" alt="MAC-NDOWS Logo" class="logo mx-auto mb-6 animate-pulse">

    <!-- Header -->
    <div>
      <h1 class="text-5xl font-bold text-white leading-tight mb-2">WELCOME TO MAC-NDOWS</h1>
      <p class="text-lg text-gray-300">
        BY <a href="https://www.linkedin.com/in/leslie-khanye" target="_blank" class="underline text-blue-400 hover:text-blue-300 transition-colors">LESLIE KHANYE</a>
      </p>
    </div>

    <!-- Description -->
    <div class="text-gray-400 space-y-4 max-w-2xl mx-auto">
      <p class="text-lg">A WEB-BASED VERSION OF (IF WINDOWS AND APPLE HAD A CHILD AND IT TURNED OUT CUTE, BUT REBELLIOUS).</p>
      <p class="text-sm italic text-yellow-300">
        (NOT OPTIMIZED FOR PHONES. HOW ABOUT TRYING A BACKFLIP INSTEAD?)
      </p>
    </div>

    <!-- Terms & Conditions -->
    <div class="text-gray-300 text-sm">
      <p class="font-bold mb-2">Before you continue, please review the Terms and Conditions:</p>
      <button 
        id="open-terms" 
        class="underline text-blue-400 hover:text-blue-300 transition-colors">
        View Terms and Conditions
      </button>
    </div>

    <!-- Continue Button -->
    <a href="/register" class="inline-block">
      <button 
        id="continue-btn" 
        class="minion-btn px-8 py-3 font-semibold text-lg rounded-full shadow-lg cursor-not-allowed opacity-50 transition-all duration-300"
        disabled>
        CONTINUE
      </button>
    </a>
  </div>

  <!-- Modal -->
  <div id="terms-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
    <div class="bg-gray-800 p-8 rounded-lg text-left max-w-lg space-y-4 text-gray-300 glass-effect">
      <h2 class="text-2xl font-bold text-white">TERMS AND CONDITIONS</h2>
      <ul class="list-disc list-inside space-y-2">
        <li>Leslie is Cute</li>
        <li>Leslie is Strong</li>
        <li>Leslie is Awesome</li>
      </ul>
      <div class="flex items-center space-x-3 mt-6">
        <input 
          type="checkbox" 
          id="accept-terms" 
          class="h-5 w-5 text-yellow-400 focus:ring-yellow-400 rounded">
        <label for="accept-terms" class="text-gray-300">Agree To All</label>
      </div>
   
      <button
        id="close-terms" 
        class="bg-gray-700 px-6 py-2 rounded-full text-gray-300 hover:bg-gray-600 transition-colors mt-4">
        Close
      </button>
    </div>
  </div>

  <!-- Footer -->
  <footer class="fixed bottom-4 text-gray-500 text-sm italic">
    NB: NOT AFFILIATED WITH MINIONS, BUT I'D TOTALLY STEAL THE MOON.
  </footer>

  <script>
    // Get elements
    const openTermsBtn = document.getElementById('open-terms');
    const closeTermsBtn = document.getElementById('close-terms');
    const termsModal = document.getElementById('terms-modal');
    const acceptTerms = document.getElementById('accept-terms');
    const continueBtn = document.getElementById('continue-btn');

    // Open modal
    openTermsBtn.addEventListener('click', () => {
      termsModal.classList.remove('hidden');
    });

    // Close modal
    closeTermsBtn.addEventListener('click', () => {
      termsModal.classList.add('hidden');
    });

    // Enable continue button when terms are accepted
    acceptTerms.addEventListener('change', () => {
      if (acceptTerms.checked) {
        continueBtn.classList.remove('opacity-50', 'cursor-not-allowed');
        continueBtn.removeAttribute('disabled');
      } else {
        continueBtn.classList.add('opacity-50', 'cursor-not-allowed');
        continueBtn.setAttribute('disabled', true);
      }
    });

    // Close modal when clicking outside
    termsModal.addEventListener('click', (e) => {
      if (e.target === termsModal) {
        termsModal.classList.add('hidden');
      }
    });
  </script>
</body>
</html>

