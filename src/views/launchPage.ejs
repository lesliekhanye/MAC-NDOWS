<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Loading | MAC-NDOWS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" type="image/x-icon" href="/images/macndows.png">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1a1a1a 0%, #2c2c2c 100%);
    }

    .loader {
      width: 8em;
      height: 8em;
      font-size: 10px;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .loader .face {
      position: absolute;
      border-radius: 50%;
      border-style: solid;
      animation: animate 3s linear infinite;
    }

    .loader .face:nth-child(1) {
      width: 100%;
      height: 100%;
      border-color: #FFD700;
      border-width: 0.2em 0.2em 0em 0em;
      --deg: -45deg;
      animation-direction: normal;
    }

    .loader .face:nth-child(2) {
      width: 70%;
      height: 70%;
      border-color: #FFD700;
      border-width: 0.2em 0em 0em 0.2em;
      --deg: -135deg;
      animation-direction: reverse;
    }

    .loader .face .circle {
      position: absolute;
      width: 50%;
      height: 0.1em;
      top: 50%;
      left: 50%;
      background-color: #FFD700;
      transform: rotate(var(--deg));
      transform-origin: left;
    }

    .loader .face .circle::before {
      position: absolute;
      top: -0.5em;
      right: -0.5em;
      content: '';
      width: 1em;
      height: 1em;
      background-color: #FFD700;
      border-radius: 50%;
      box-shadow: 0 0 2em, 0 0 4em, 0 0 6em, 0 0 8em, 0 0 10em #FFD700;
    }

    @keyframes animate {
      to {
        transform: rotate(1turn);
      }
    }

    .minion-img {
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0% {
        transform: translatey(0px);
      }
      50% {
        transform: translatey(-20px);
      }
      100% {
        transform: translatey(0px);
      }
    }

    .glass-effect {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
  </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen text-white p-4">
  <div class="w-full max-w-4xl glass-effect rounded-2xl p-8 space-y-8 text-center">
    <!-- Loading animation -->
    <div class="loader mx-auto">
      <div class="face">
        <div class="circle"></div>
      </div>
      <div class="face">
        <div class="circle"></div>
      </div>
    </div>

    <h1 class="text-4xl font-bold leading-tight">PLEASE WAIT WHILE WE CLEAN UP THE PLACE FOR YOU :)</h1>

    <!-- Minion image -->
    <img src="/images/selfieminion.png" alt="Minion" class="minion-img w-40 mx-auto">

    <!-- Additional message -->
    <p class="text-sm text-gray-300 max-w-lg mx-auto">
      DID YOU KNOW WEB DESIGNERS ACTUALLY LIVE LONGER BECAUSE..........
      <br>
      <span class="text-yellow-300 hover:underline cursor-pointer">DM ME TO KNOW WHY</span>
    </p>

    <!-- Progress bar -->
    <div class="w-full bg-gray-700 rounded-full h-2.5 dark:bg-gray-700 max-w-md mx-auto">
      <div class="bg-yellow-400 h-2.5 rounded-full w-0" id="progress-bar"></div>
    </div>
  </div>

  <script>
    // Simulate loading progress
  const progressBar = document.getElementById('progress-bar');
  let width = 0;
  const interval = setInterval(() => {
    if (width >= 100) {
      clearInterval(interval);
      // Redirect to /desktop after loading is complete
      window.location.href = '/desktop';
    } else {
      width++;
      progressBar.style.width = width + '%';
    }
  }, 50);
  </script>
</body>
</html>

